export interface Ad {
  id: number;
  title: string;
  image: string;
  description: string;
}

export interface Product {
  id: number;
  name: string;
  image: string;
  description: string;
  price: number;
  quantity: number;
}
